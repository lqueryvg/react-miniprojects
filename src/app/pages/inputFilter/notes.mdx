import { TextLink } from "~/app/_components/TextLink";
import { App } from "./App";

## Exercise

- display a list of items and an input search field
- as the user types, displays only those items that match the search term
- display a friendly message if no items match the search

## Example

<App />
